# Copy to .env.local for development. On Vercel: Project Settings → Environment Variables
# (set Production and Preview as needed). Server-only keys must not have NEXT_PUBLIC_.

# —— ConvertKit (Kit) — server-only; never expose in client code ——
CONVERTKIT_API_KEY=

# Optional: one default form for local dev if per-intent forms are not set
CONVERTKIT_DEFAULT_FORM_ID=

# Per-intent ConvertKit form IDs (numeric). Configure automations/tags on each form in Kit.
CONVERTKIT_FORM_LOVE_RESET=
CONVERTKIT_FORM_INTENTIONAL_SPACE=
CONVERTKIT_FORM_CT_COLLAB=
CONVERTKIT_FORM_FOREVER_DAY=
CONVERTKIT_FORM_FOREVER_TABLE=
CONVERTKIT_FORM_CIRCLE=
CONVERTKIT_FORM_SPEAKING=
CONVERTKIT_FORM_GENERAL=
CONVERTKIT_FORM_NEWSLETTER=

# Optional: tag subscribe by numeric ID (see Kit dashboard). Env key pattern:
# CONVERTKIT_TAG_ID_LOVE_RESET, CONVERTKIT_TAG_ID_CT_COLLAB, etc.
#
# In Kit, create custom fields: subscription_subject (for all /api/subscribe intents),
# event_details (optional, for Speaking form). Use merge tags in confirmation emails.

# —— Resend (contact form) — server-only ——
MY_RESEND_API=
# Verified domain sender, e.g. "Tobi Yusuf site <hello@yourdomain.com>"
RESEND_FROM=

# Tally webhooks (shared signing secret), e.g.:
# - POST /api/tally-waitlist (Choosing Us waitlist confirmation)
# - POST /api/tally-inside-mind (Inside The Mind notify confirmation)
# Set the same secret in Tally Webhooks → signing secret. Leave unset locally to skip verify.
TALLY_WEBHOOK_SIGNING_SECRET=

# —— Public URLs (safe for browser) ——
# Canonical production origin (no trailing slash) — used for SEO, metadataBase, Open Graph.
# Set on Vercel Production. Match Google Search Console to this host (www) and submit:
# https://www.tobiyusuf.com/sitemap.xml
NEXT_PUBLIC_SITE_URL=https://www.tobiyusuf.com
NEXT_PUBLIC_SCHEDULING_URL= https://therelatablewife.as.me/
NEXT_PUBLIC_TICKET_CANDY_URL=
NEXT_PUBLIC_SUBSTACK_URL=https://substack.com/@mrstobiyusuf?r=72pimk&utm_medium=ios&utm_source=stories&shareImageVariant=light
# Intentional Space waitlist (Tally); defaults to https://tally.so/r/J9DKpK if unset
NEXT_PUBLIC_TALLY_INTENTIONAL_SPACE_WAITLIST_URL=
NEXT_PUBLIC_INSTAGRAM_URL=https://www.instagram.com/mrstobiyusuf
NEXT_PUBLIC_LINKEDIN_URL=https://www.linkedin.com/in/tobi-yusuf-68813812b
